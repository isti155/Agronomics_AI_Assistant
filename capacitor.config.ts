import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.digital.agronomist',
  appName: 'digital-agronomist',
  webDir: 'dist'
};

export default config;
